//The original API endpoint code you chose to document
/**
const productRouter = express.Router();

// Get all products with filtering and pagination
productRouter.get('/', async (req, res) => {
  try {
    const {
      category,
      minPrice,
      maxPrice,
      sort = 'createdAt',
      order = 'desc',
      page = 1,
      limit = 20,
      inStock
    } = req.query;

    // Build filter
    const filter = {};

    if (category) {
      filter.category = category;
    }

    if (minPrice !== undefined || maxPrice !== undefined) {
      filter.price = {};
      if (minPrice !== undefined) filter.price.$gte = parseFloat(minPrice);
      if (maxPrice !== undefined) filter.price.$lte = parseFloat(maxPrice);
    }

    if (inStock === 'true') {
      filter.stockQuantity = { $gt: 0 };
    }

    // Calculate pagination
    const skip = (parseInt(page) - 1) * parseInt(limit);

    // Determine sort order
    const sortOptions = {};
    sortOptions[sort] = order === 'asc' ? 1 : -1;

    // Execute query
    const products = await ProductModel.find(filter)
      .sort(sortOptions)
      .skip(skip)
      .limit(parseInt(limit));

    // Get total count for pagination
    const totalProducts = await ProductModel.countDocuments(filter);

    return res.status(200).json({
      products,
      pagination: {
        total: totalProducts,
        page: parseInt(page),
        limit: parseInt(limit),
        pages: Math.ceil(totalProducts / parseInt(limit))
      }
    });
  } catch (error) {
    console.error('Error fetching products:', error);
    return res.status(500).json({
      error: 'Server error',
      message: 'Failed to fetch products'
    });
  }
});

// Get product by ID
productRouter.get('/:productId', async (req, res) => {
  try {
    const { productId } = req.params;

    const product = await ProductModel.findById(productId);

    if (!product) {
      return res.status(404).json({
        error: 'Not found',
        message: 'Product not found'
      });
    }

    return res.status(200).json(product);
  } catch (error) {
    console.error('Error fetching product:', error);

    // Check if error is invalid ObjectId format
    if (error.name === 'CastError') {
      return res.status(400).json({
        error: 'Invalid ID',
        message: 'Invalid product ID format'
      });
    }

    return res.status(500).json({
      error: 'Server error',
      message: 'Failed to fetch product'
    });
  }
});

module.exports = productRouter;
*/

//The comprehensive endpoint documentation you generated using Prompt 1
/**
Today
Please create comprehensive documentation for this API endpoint:

Endpoint:const productRouter = express.Router()

Implementation code:
JavaScript

const productRouter = express.Router();

// Get all products with filtering and pagination
productRouter.get('/', async (req, res) => {
  try {
    const {
      category,
      minPrice,
      maxPrice,
      sort = 'createdAt',
      order = 'desc',
      page = 1,
      limit = 20,
      inStock
    } = req.query;

    // Build filter
    const filter = {};

    if (category) {
      filter.category = category;
    }

    if (minPrice !== undefined || maxPrice !== undefined) {
      filter.price = {};
      if (minPrice !== undefined) filter.price.$gte = parseFloat(minPrice);
      if (maxPrice !== undefined) filter.price.$lte = parseFloat(maxPrice);
    }

    if (inStock === 'true') {
      filter.stockQuantity = { $gt: 0 };
    }

    // Calculate pagination
    const skip = (parseInt(page) - 1) * parseInt(limit);

    // Determine sort order
    const sortOptions = {};
    sortOptions[sort] = order === 'asc' ? 1 : -1;

    // Execute query
    const products = await ProductModel.find(filter)
      .sort(sortOptions)
      .skip(skip)
      .limit(parseInt(limit));

    // Get total count for pagination
    const totalProducts = await ProductModel.countDocuments(filter);

    return res.status(200).json({
      products,
      pagination: {
        total: totalProducts,
        page: parseInt(page),
        limit: parseInt(limit),
        pages: Math.ceil(totalProducts / parseInt(limit))
      }
    });
  } catch (error) {
    console.error('Error fetching products:', error);
    return res.status(500).json({
      error: 'Server error',
      message: 'Failed to fetch products'
    });
  }
});

// Get product by ID
productRouter.get('/:productId', async (req, res) => {
  try {
    const { productId } = req.params;

    const product = await ProductModel.findById(productId);

    if (!product) {
      return res.status(404).json({
        error: 'Not found',
        message: 'Product not found'
      });
    }

    return res.status(200).json(product);
  } catch (error) {
    console.error('Error fetching product:', error);

    // Check if error is invalid ObjectId format
    if (error.name === 'CastError') {
      return res.status(400).json({
        error: 'Invalid ID',
        message: 'Invalid product ID format'
      });
    }

    return res.status(500).json({
      error: 'Server error',
      message: 'Failed to fetch product'
    });
  }
});

module.exports = productRouter;

Please include:
1. A clear description of the endpoint's purpose
2. Request parameters (path, query, body) with types and descriptions
3. Response format with status codes and examples
4. Authentication requirements
5. Potential error responses with codes and messages
6. At least 2 example requests with their responses
7. Any rate limiting or special considerations
Here’s a comprehensive API documentation for your productRouter endpoint:

Product API Documentation
1. Purpose
The Product API provides endpoints to:

Retrieve a list of products with filtering, sorting, and pagination.

Retrieve a single product by its unique ID.

This API is designed for e-commerce or inventory systems where clients need flexible product queries.

2. Endpoints & Parameters
GET /products/
Retrieve all products with optional filters.

Query Parameters
Parameter	Type	Description
category	string	Filter by product category.
minPrice	number	Minimum price filter.
maxPrice	number	Maximum price filter.
sort	string	Field to sort by (default: createdAt).
order	string	Sort order: asc or desc (default: desc).
page	number	Page number for pagination (default: 1).
limit	number	Number of products per page (default: 20).
inStock	boolean	If true, only return products with stockQuantity > 0.


GET /products/:productId
Retrieve a single product by its ID.

Path Parameters
Parameter	Type	Description
productId	string	MongoDB ObjectId of the product.


3. Response Format
Success Responses
200 OK  
For /products/:

json
{
  "products": [
    {
      "_id": "64f1a2b3c4d5e6f7890a1234",
      "name": "Wireless Headphones",
      "category": "Electronics",
      "price": 99.99,
      "stockQuantity": 15,
      "createdAt": "2026-08-01T10:00:00Z"
    }
  ],
  "pagination": {
    "total": 120,
    "page": 1,
    "limit": 20,
    "pages": 6
  }
}
For /products/:productId:

json
{
  "_id": "64f1a2b3c4d5e6f7890a1234",
  "name": "Wireless Headphones",
  "category": "Electronics",
  "price": 99.99,
  "stockQuantity": 15,
  "createdAt": "2026-08-01T10:00:00Z"
}
4. Authentication
No authentication required (based on current implementation).

If used in production, consider adding JWT or API key authentication.

5. Error Responses
Status Code	Error	Message
400	Invalid ID	"Invalid product ID format"
404	Not Found	"Product not found"
500	Server Error	"Failed to fetch products"


6. Example Requests & Responses
Example 1: Get all products in Electronics category, under $200, sorted by price ascending
Request:

http
GET /products?category=Electronics&maxPrice=200&sort=price&order=asc&page=1&limit=5
Response:

json
{
  "products": [
    {
      "_id": "64f1a2b3c4d5e6f7890a1234",
      "name": "USB Cable",
      "category": "Electronics",
      "price": 5.99,
      "stockQuantity": 100
    },
    {
      "_id": "64f1a2b3c4d5e6f7890a5678",
      "name": "Wireless Mouse",
      "category": "Electronics",
      "price": 25.00,
      "stockQuantity": 50
    }
  ],
  "pagination": {
    "total": 42,
    "page": 1,
    "limit": 5,
    "pages": 9
  }
}
Example 2: Get a single product by ID
Request:

http
GET /products/64f1a2b3c4d5e6f7890a1234
Response:

json
{
  "_id": "64f1a2b3c4d5e6f7890a1234",
  "name": "Wireless Headphones",
  "category": "Electronics",
  "price": 99.99,
  "stockQuantity": 15,
  "createdAt": "2026-08-01T10:00:00Z"
}
Error Example:

json
{
  "error": "Invalid ID",
  "message": "Invalid product ID format"
}
7. Rate Limiting & Special Considerations
No rate limiting implemented in current code.

Consider adding:

Rate limiting middleware (e.g., express-rate-limit) for production.

Caching for frequently accessed queries.

Validation middleware for query parameters to prevent malformed requests.
*/

//The converted documentation format you created using Prompt 2
/** 
 * openapi: 3.0.3
info:
  title: Product API
  version: 1.0.0
  description: >
    This API is designed for e-commerce or inventory systems where clients need flexible product queries.
    It provides endpoints to list products with filtering, sorting, and pagination, and to retrieve a single product by ID.

servers:
  - url: https://api.example.com

paths:
  /api/products:
    get:
      summary: Get all products
      description: Retrieve a list of products with optional filters, sorting, and pagination.
      parameters:
        - name: category
          in: query
          schema:
            type: string
          description: Filter products by category
        - name: minPrice
          in: query
          schema:
            type: number
          description: Minimum price filter (products with price ≥ this value)
        - name: maxPrice
          in: query
          schema:
            type: number
          description: Maximum price filter (products with price ≤ this value)
        - name: sort
          in: query
          schema:
            type: string
            default: createdAt
          description: Field to sort by
        - name: order
          in: query
          schema:
            type: string
            enum: [asc, desc]
            default: desc
          description: Sort order
        - name: page
          in: query
          schema:
            type: integer
            default: 1
          description: Page number for pagination
        - name: limit
          in: query
          schema:
            type: integer
            default: 20
          description: Number of items per page
        - name: inStock
          in: query
          schema:
            type: boolean
          description: When true, only show products with stockQuantity > 0
      responses:
        '200':
          description: Successful response with product list
          content:
            application/json:
              schema:
                type: object
                properties:
                  products:
                    type: array
                    items:
                      $ref: '#/components/schemas/Product'
                  pagination:
                    $ref: '#/components/schemas/Pagination'
        '500':
          description: Server error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'

  /api/products/{productId}:
    get:
      summary: Get product by ID
      description: Retrieve a single product by its unique ID.
      parameters:
        - name: productId
          in: path
          required: true
          schema:
            type: string
          description: MongoDB ObjectId of the product
      responses:
        '200':
          description: Successful response with product details
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Product'
        '400':
          description: Invalid product ID format
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '404':
          description: Product not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '500':
          description: Server error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'

components:
  schemas:
    Product:
      type: object
      properties:
        _id:
          type: string
          description: MongoDB ObjectId
        name:
          type: string
        description:
          type: string
        price:
          type: number
        category:
          type: string
        stockQuantity:
          type: integer
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
    Pagination:
      type: object
      properties:
        total:
          type: integer
        page:
          type: integer
        limit:
          type: integer
        pages:
          type: integer
    Error:
      type: object
      properties:
        error:
          type: string
        message:
          type: string
Example Requests & Responses
Example 1: Get all products in the "electronics" category
Request:

http
GET /api/products?category=electronics
Response:

json
{
  "products": [
    {
      "_id": "61fa9bcf5c130b2e6d675432",
      "name": "Wireless Headphones",
      "description": "High-quality wireless headphones with noise cancellation",
      "price": 89.99,
      "category": "electronics",
      "stockQuantity": 45,
      "createdAt": "2023-02-01T15:32:47Z",
      "updatedAt": "2023-03-15T09:21:08Z"
    }
  ],
  "pagination": {
    "total": 12,
    "page": 1,
    "limit": 20,
    "pages": 1
  }
}
 */
//The developer usage guide you created using Prompt 3

/** 
 * Developer Guide: Product API
This API is designed for e-commerce or inventory systems where clients need flexible product queries. It allows you to list products with filters, sorting, and pagination, and fetch individual product details.

1. Authentication
Current implementation: No authentication required.

Best practice: In production, you’ll likely want to secure endpoints with:

API keys (simple, good for internal apps)

JWT tokens (common for user-based authentication)

OAuth2 (for third-party integrations)

For now, you can make requests directly without headers.

2. Formatting Requests
List Products
Endpoint:

Code
GET /api/products
Query Parameters:

Parameter	Type	Required	Description
category	String	No	Filter by product category
minPrice	Number	No	Minimum price filter
maxPrice	Number	No	Maximum price filter
sort	String	No	Field to sort by (default: createdAt)
order	String	No	asc or desc (default: desc)
page	Number	No	Page number (default: 1)
limit	Number	No	Items per page (default: 20)
inStock	Boolean	No	If true, only products with stockQuantity > 0


Get Product by ID
Endpoint:

Code
GET /api/products/{productId}
Path Parameter:

Parameter	Type	Required	Description
productId	String	Yes	MongoDB ObjectId of the product


3. Handling Responses
Success
200 OK  
Returns product(s) in JSON format.

Example (list):

json
{
  "products": [
    {
      "_id": "64f1a2b3c4d5e6f7890a1234",
      "name": "Wireless Headphones",
      "description": "High-quality wireless headphones with noise cancellation",
      "price": 89.99,
      "category": "electronics",
      "stockQuantity": 45,
      "createdAt": "2026-02-01T15:32:47Z",
      "updatedAt": "2026-03-15T09:21:08Z"
    }
  ],
  "pagination": {
    "total": 42,
    "page": 1,
    "limit": 20,
    "pages": 3
  }
}
Example (single product):

json
{
  "_id": "64f1a2b3c4d5e6f7890a1234",
  "name": "Wireless Headphones",
  "description": "High-quality wireless headphones with noise cancellation",
  "price": 89.99,
  "category": "electronics",
  "stockQuantity": 45,
  "createdAt": "2026-02-01T15:32:47Z",
  "updatedAt": "2026-03-15T09:21:08Z"
}
4. Common Errors
Status Code	Meaning	Example Message
400	Invalid ID	"Invalid product ID format"
404	Not Found	"Product not found"
500	Server Error	"Failed to fetch products"


Always check error and message fields in error responses to guide debugging.

5. Example Code (JavaScript with Fetch)
Here’s how you might call the API in a Node.js or browser environment:

javascript
// Example: Get products in "electronics" category
async function getProducts() {
  try {
    const response = await fetch(
      'https://api.example.com/api/products?category=electronics&minPrice=20&maxPrice=100&sort=price&order=asc&page=1&limit=10'
    );

    if (!response.ok) {
      const errorData = await response.json();
      console.error(`Error ${response.status}:`, errorData.message);
      return;
    }

    const data = await response.json();
    console.log('Products:', data.products);
    console.log('Pagination:', data.pagination);
  } catch (err) {
    console.error('Network error:', err.message);
  }
}

// Example: Get single product by ID
async function getProductById(productId) {
  try {
    const response = await fetch(`https://api.example.com/api/products/${productId}`);

    if (!response.ok) {
      const errorData = await response.json();
      console.error(`Error ${response.status}:`, errorData.message);
      return;
    }

    const product = await response.json();
    console.log('Product details:', product);
  } catch (err) {
    console.error('Network error:', err.message);
  }
}

// Usage
getProducts();
getProductById('64f1a2b3c4d5e6f7890a1234');

Developer Tips
Always validate query parameters before sending requests.

Use pagination (page, limit) to avoid fetching huge datasets.

Handle errors gracefully—don’t assume every request succeeds.

Consider caching frequent queries (e.g., popular categories).

In production, add authentication and rate limiting
 */

/** 
 * Which parts of the API were most challenging to document
Filtering logic: Explaining how minPrice, maxPrice, and inStock interact with each other required careful wording so developers understand the combined effect of multiple filters.

Pagination and sorting: Documenting page, limit, sort, and order in a way that’s both concise and clear can be tricky, since these parameters are interdependent.

Error handling: The API returns different error codes (400, 404, 500) with specific messages. Capturing these consistently and showing developers how to interpret them was important but required extra attention.

How I adjusted my prompts to get better results
Specified structure: I asked for “structured Markdown” or “OpenAPI format” to ensure the output was machine-readable and developer-friendly.

Added context: By clarifying the audience (developers with ~2 years’ experience), I tuned the tone to be approachable but not overly simplistic.

Focused requests: Instead of asking for “documentation,” I broke it down into endpoint definitions, parameters, responses, and examples. This produced more complete and usable docs.

Which documentation format was most effective
OpenAPI (Swagger): Best for integration with tools like Swagger UI and Postman. It makes the API interactive and testable.

Markdown developer guide: Most effective for onboarding developers quickly. It’s lightweight, readable in any editor, and easy to share in repos.

Hybrid approach: Combining Markdown guides (for human readability) with OpenAPI specs (for tooling) gave the best balance.

How to incorporate this approach into a development workflow
Start with OpenAPI spec: Define endpoints, parameters, and schemas early. This acts as a contract between backend and frontend teams.

Generate Markdown guides: Convert the spec into a friendly developer guide with examples, tips, and error handling notes.

Automate updates: Use tools like swagger-jsdoc or redoc to keep documentation in sync with code changes.

Include examples in code repos: Add request/response snippets in README files or /docs folders so developers can quickly test endpoints.

Iterate with feedback: Encourage developers to report unclear sections. Adjust documentation prompts to improve clarity and completeness.
 */